# Dynamic Keycards 사용 설명서

버전 0.0.2 기준. 모든 조작은 **우클릭**이며, "웅크리기"는 Shift를 누른 상태를 뜻합니다.

---

## 1. 아이템

### 키 카드 (16색)
- 바닐라 16색 염료 색상별로 존재합니다. 처음 만든 카드는 **빈 카드**(키 없음)입니다.
- 카드 리더기 **등록** 또는 카드 복제기 **복제**를 거치면 카드에 **고유 키**가 찍힙니다
  (툴팁에 `키: xxxxxxxx`로 표시). 같은 색이라도 키가 다르면 다른 카드입니다.
- 조합대에서 **카드 + 아무 염료**(위치 무관)로 색을 바꿀 수 있습니다.
  ⚠️ 염색하면 키가 지워져 **빈 카드로 초기화**됩니다.
- 키가 찍힌 카드 여러 장이 한 스택으로 등록되면 스택 전체가 같은 키를 갖습니다.

### 골드 키 카드 (마스터 카드)
- 등록·귀속 여부와 상관없이 **모든 카드 리더기를 통과**시킵니다.
- 남의 리더기라도 **등록모드를 켜고 끌 수** 있습니다 (아래 리더기 표 참고).
- 키가 찍히지 않고, 등록도 복제도 되지 않습니다. 현재는 크리에이티브 전용입니다.

---

## 2. 카드 리더기 (4종)

**삽입형 · 터치형 · 스와이프형 · 고급형** — 기능은 4종 모두 동일하고 외형과 상태등만 다릅니다.
바닥/벽/천장에 설치할 수 있습니다.

### 기본 개념
- 설치하는 순간 **설치한 플레이어에게 귀속**됩니다(= 소유자).
- 소유자가 카드를 **등록**하면, 그 카드로 리더기를 작동시킬 수 있습니다.
- 등록된 카드로 서서 우클릭하면 **3초 레드스톤 펄스**(신호 세기 15, 부착면 강충전)를 냅니다.
- 하나의 카드를 여러 리더기에, 하나의 리더기에 여러 카드를 등록할 수 있습니다.
- 리더기를 부수면 귀속·등록 정보가 사라집니다.

### 상태등
| 표시 | 의미 |
|---|---|
| 꺼짐 | 대기 |
| 초록 점등 | 통과 (펄스 출력 중, 3초) |
| 빨강 점등 | 거부 (1초) |
| 등록 표시 깜빡임 | 등록모드 활성화 (고급형은 파란 화면 + LED 깜빡임) |

### 상호작용 표

**평상시 (등록모드 꺼짐)**

| 행동 | 소유자 | 비소유자 |
|---|---|---|
| 서서 + 맨손 | 무반응 | 무반응 |
| 웅크림 + 맨손 | **등록모드 켜기** ("소유한 키 카드를 등록하세요") | "귀속되지 않은 리더기입니다" (빨강) |
| 서서 + 등록된 카드 | **통과** (펄스, 자막 없음) | **통과** |
| 서서 + 미등록/빈 카드 | "등록되지 않은 카드입니다" (빨강) | 동일 |
| 웅크림 + 카드 | 무반응 | 무반응 |
| 서서 + 골드 카드 | **통과** | **통과** |
| 웅크림 + 골드 카드 | **등록모드 켜기** | **등록모드 켜기** |
| 그 외 아이템 | 무반응 | 무반응 |

**등록모드 중** (소유자/비소유자 동일)

| 행동 | 결과 |
|---|---|
| 맨손 클릭 (서서/웅크림 무관) | 등록모드 해제 ("등록이 중단되었습니다") |
| 웅크림 + 새 카드 | **등록** — 빈 카드면 키가 새로 찍힘 ("등록이 완료되었습니다", 초록) |
| 웅크림 + 이미 등록된 카드 | **등록 해제** ("등록이 해제되었습니다") |
| 서서 + 카드 | 무반응 |
| 서서 + 골드 카드 | 등록모드 해제 |
| 웅크림 + 골드 카드 | **전체 초기화** — 등록된 카드 전부 삭제 ("카드 리더기가 초기화되었습니다") |

---

## 3. 카드 복제기

키가 찍힌 카드를 빈 카드에 복사하는 장치입니다. 8×8 크기의 패드로 바닥/벽/천장에 설치하며,
**레드스톤 신호는 내지 않습니다.** 누구나 사용할 수 있습니다(귀속 없음).

### 사용 순서
1. **원본 카드**(키가 찍힌 카드)를 들고 웅크려 우클릭
   → 초록 라이트가 깜빡이며 "빈 카드를 들고 입력하세요"
2. **빈 카드**를 들고 웅크려 우클릭
   → 초록 점등(1.5초) + "복제가 완료되었습니다" — 빈 카드에 원본과 같은 키가 찍힙니다.
   카드 색은 그대로 유지되므로 색이 달라도 같은 키를 가질 수 있습니다.

### 취소와 안내
- **맨손 클릭**(서서/웅크림 무관): 진행 중이면 취소("복제가 중단되었습니다"),
  대기 상태에서 웅크려 클릭하면 안내("복제할 카드를 들고 입력하세요").

### 거부 (빨강 1초 점등 + 빨간 자막)
| 상황 | 자막 |
|---|---|
| 골드 카드를 원본/대상으로 사용 | "골드 키 카드는 복제할 수 없습니다" |
| 빈 카드를 원본으로 입력 | "빈 카드는 복제할 수 없습니다" |
| 키 있는 카드를 대상으로 입력 | "빈 카드가 아닙니다" |

거부되어도 대기 중인 원본 키는 유지되며, 빨강 점등이 끝나면 다시 초록 깜빡임으로 돌아갑니다.

---

## 4. 조합법

### 카드 리더기 (모양 조합)
가운데 줄과 아랫줄은 공통: **레드스톤 – 레드스톤 조명 – 레드스톤 / 철괴 ×3** (고급형만 금괴 ×3)

| 종류 | 윗줄 |
|---|---|
| 삽입형 | (빈칸) 호퍼 (빈칸) |
| 터치형 | 검은색 색유리 ×3 |
| 스와이프형 | 레일 ×3 |
| 고급형 | 자수정 조각 ×3 |

### 키 카드
- **하얀색 키 카드**: 레드스톤 ×3 / (빈칸) 종이 (빈칸) / 무거운 무게 압력판 ×3
- **염색**: 카드 + 염료 (비형태 조합, 위치 무관) → 해당 색 카드 (키 초기화됨)

### 카드 복제기
(빈칸) 아무 키 카드 (빈칸) / 금괴 – 흑요석 – 금괴 / 흑요석 ×3

### 골드 키 카드
조합법 없음 (크리에이티브 전용).

---

## 5. 호환성 메모
- Carry On 등 "웅크려 우클릭"을 쓰는 집기 모드가 있어도 리더기/복제기는 들리지 않습니다.
- 철제 광물 재료는 태그(`c:ingots/iron` 등) 기반이라 타 모드의 동등 재료로도 조합됩니다.
